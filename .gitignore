# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
out/
# build/ 는 electron-builder 의 buildResources (icons, entitlements 등) — track 필요

# Electron / Vite
.vite/
.cache/
release/

# IDE / Editor
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
*.iml
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Testing
coverage/
.nyc_output/

# Playwright (E2E)
test-results/
playwright-report/
playwright/.cache/

# Environment / secrets
.env
.env.local
.env.*.local
*.pem
*.key

# OS
Thumbs.db

# Tools
.eslintcache
.tsbuildinfo

# v0.14.0 (A ABI Hardening) — ensure-abi.cjs 의 ABI 토글 캐시 (dev / test ABI
# 일치 검증 결과). machine-specific (binding mtime + Electron major + Node major)
# 이라 repo 추적 X. 다음 npm run dev / npm test 시 자동 재생성.
.ensure-abi-state.json

# Captures (analysis 결과 — 별도 보관)
captures/

# Local DB / cache
*.sqlite
*.sqlite-shm
*.sqlite-wal
sessions.sqlite*

# OMC orchestration artifacts (Claude Code multi-agent runtime — not part of repo)
.omc/
.omx/
