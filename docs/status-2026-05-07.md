# Dreampia-Dev — 작업 현황 스냅샷

> **기준일**: 2026-05-07
> **마지막 commit**: `560f26e` (v1.4.8 — workspace_id sha256 default + backfill prompt modal)
> **package.json**: `1.4.8`
> **origin/main 대비**: +109 commits ahead (push 필요)
> **CHANGELOG.md**: 5,344 lines

본 문서는 **현재까지 무엇이 끝났고 무엇이 남았는지** 한눈에 보기
위한 단일 진실 (single source of truth).

---

## 1. 트랙 구조 (v1.x)

Dreampia-Dev 는 **5개 동시 진행 트랙**으로 v1.x 가 구성됨. 각 트랙은
독립적인 슬롯 시리즈를 가지며 commit 마다 atomic 하게 land.

| 트랙 | 영역 | 마지막 슬롯 | 상태 |
|---|---|---|---|
| **v1.0.x** | Core stability | v1.0.15 | 🟢 closed |
| **v1.1.x** | Permission + plugin | v1.1.28 | 🟢 closed |
| **v1.2.x** | Direct API foundation | v1.2.5 | 🟢 closed |
| **v1.3.x** | A11y + i18n + automation infra | v1.3.7 | 🟢 closed |
| **v1.4.x** | Storage debt (B-1~B-4) | **v1.4.8** | 🟡 active |
| **v1.5.x** | Media + Vision + Direct API UI | v1.5.7 | 🟢 closed |
| **v1.6.x** | Browser + Plugin hooks + Chat UX | **v1.6.19** | 🟡 active |
| **v1.7.x** | Telemetry + Automation + Settings i18n | **v1.7.25** | 🟡 active |

**활성 트랙 (다음 batch 후보):** v1.4.x / v1.6.x / v1.7.x

---

## 2. 직전 세션에서 land 한 슬롯 (4 commits)

### v1.7.23 — Handler registry + LLM prompt handler (`2b56f7a`)
- 신규: `HandlerRegistry` (name → async handler) + `noopLogHandler` fallback.
- 신규: `llmPromptHandler` ('llm-prompt') — config.prompt 를 활성
  provider 로 stream + audit 캡처.
- 신규 IPC: `automation/list-handlers`.
- 확장: `AutomationRule` 에 `handler_name` + `handler_config` 추가, audit
  event 의 `output` 캡처.
- 신규 unit: 15 tests PASS.

### v1.7.24 — Shell exec handler (`2f73e4b`)
- 신규: `shellExecHandler` ('shell-exec') — `child_process.spawn` (shell:false,
  args array only — injection 차단).
- 보안: `automation_shell_enabled` settings opt-in (default false).
- 가드: timeout SIGKILL (default 30s), 64KB stdout/stderr cap.
- 신규 unit: 10 tests PASS.

### v1.7.25 — IPC trigger handler + Modal handler picker UI (`a718a83`)
- 신규: `ipcTriggerHandler` ('ipc-trigger') — `automation/` prefix
  allow-list. mainWindow.webContents.send 라우팅.
- 신규: `setIpcTriggerEmitter` injection in main/index.ts boot.
- UI: `AutomationModal` 에 handler dropdown + JSON config textarea +
  handler badge. i18n ko/en 6 키 추가.
- preload `automation.listHandlers()` 노출.
- 신규 unit: 9 tests + 4 IPC tests PASS.

### v1.4.8 — workspace_id sha256 default + backfill prompt modal (`560f26e`)
- 신규: `detectLegacyWorkspaceIds(db)` read-only.
- 신규 IPC: `app:check-workspace-backfill`, `app:dismiss-workspace-backfill`.
- 신규: `BackfillPromptModal.tsx` (count / conflict / 3-button: run/later/dismiss).
- App.tsx 부팅 useEffect → flag false + legacy>0 시 modal mount.
- App.tsx createDemoSession: `workspaceIdFor` → `workspaceIdForSha256`.
- Settings: `workspace_backfill_done?: boolean` 추가.
- i18n ko/en 12 키 추가.
- 신규 unit: 12 tests PASS.

**누적 변화:** 4 commits / +50 unit tests / typecheck clean / lint clean.

---

## 3. 테스트 baseline

| 지표 | 값 |
|---|---|
| **passed** | 1,988 |
| **failed (baseline)** | 7 |
| **신규 추가 (이번 세션)** | +50 |
| **typecheck** | clean |
| **lint** | clean (변경 파일) |

### Baseline 7 fail 내역 (이번 작업과 무관)
- `tests/main/ipc.workspace.test.ts` — `workspace/pick-folder` 5 tests
- `tests/storage/SessionStore.test.ts` — migrations 2 tests

---

## 4. 인프라 및 영구 상태

### 4.1 자동화 스택 (v1.7.x 시리즈 완성)
```
AutomationManager (v1.3.7 + v1.7.3 cron)
  ├── AutomationHttpListener (v1.7.2 webhook)
  ├── HandlerRegistry (v1.7.23)
  │   ├── noop-log     (default fallback)
  │   ├── llm-prompt   (v1.7.23)
  │   ├── shell-exec   (v1.7.24, opt-in)
  │   └── ipc-trigger  (v1.7.25)
  ├── settings.automation_rules 영속 (v1.7.14)
  └── AutomationModal UI (v1.7.4 + v1.7.25 picker)
```

### 4.2 Workspace ID 마이그레이션 (v1.4 시리즈)
```
v1.4.0 — sha256 helper + backfill function
v1.4.1 — permission_grants.id TEXT rebuild
v1.4.2 — sessions.conversation columns promote
v1.4.3 — down migration + revertTo API
v1.4.4 — _extra namespace introduction
v1.4.5 — _extra columns expansion
v1.4.6 — workspaces._extra
v1.4.7 — DiagnoseSettings backfill button
v1.4.8 — sha256 default switch + boot prompt modal  ← 현재
```

### 4.3 Settings i18n 완성도 (v1.7.x 시리즈)
- ✅ App.tsx toast (v1.7.17)
- ✅ McpSettings (v1.7.18)
- ✅ UsageSettings (v1.7.19)
- ✅ SlashHelpModal (v1.7.20)
- ✅ Sidebar coming_soon helper 제거 (v1.7.21)
- ✅ Settings toast detail 정규화 (v1.7.22)
- ✅ AutomationModal handler picker (v1.7.25)
- ⏳ ChatPanel 잔여 (v1.6.20 후보)
- ⏳ AboutPanel / OnboardingWizard 일부 (v1.6.21 후보)

---

## 5. 미해결 작업 더미 (working tree)

```
Modified: tests/storage/permissionGrants.idText.test.ts  (이번 작업과 무관)
Untracked: settings.json                                 (build artifact)
```

**액션 필요:** 다음 세션 시작 전 위 두 항목을 stash 또는 분리 commit.

---

## 6. 주요 의존성 그래프 (다음 batch 영향)

```
v1.4.8 (sha256 default)
  ↓ depends
v1.4.9 — workspace cache invalidation (App.tsx 의 in-memory map)
  ↓ depends
v1.4.10 — workspace UNIQUE(root) conflict graceful handling

v1.7.25 (handler picker UI)
  ↓ depends
v1.7.26 — Audit log persistence (handler output → AuditLogStore.recordEvent)
  ↓ depends
v1.7.27 — Audit log viewer panel

v1.7.x (자동화 인프라 완성)
  ↓ enables
v1.7.28 — Automation rule import/export JSON

v1.4.4-1.4.6 (_extra namespace)
  ↓ enables (Option D)
v1.4.9 또는 v1.8.0 — _extra → column promote audit
```

---

## 7. 다음 batch 후보 (미정의, roadmap 문서 참조)

자세한 슬롯 정의 + 우선순위 + 예상 시간은 별도 문서:
**`docs/v1.x-next-batch.md`** 참조.

빠른 요약:
- **그룹 A (독립 5)**: v1.4.9 / v1.6.20 / v1.7.26 / v1.4.10 / v1.7.27
- **그룹 B (독립 5)**: v1.7.28 / v1.6.21 / v1.4.11 / v1.7.29 / v1.6.22
- **그룹 C (순차, _extra audit)**: v1.8.0 → v1.8.1 → v1.8.2

---

## 8. 환경 / 도구 상태

- **OMC delegation**: 활성화 여부 미확인 (이전 세션은 차단됨).
  활성 시 `Task` 도구로 서브에이전트 병렬 위임 가능. 비활성 시 메인
  세션 직접 편집.
- **HUD**: 미활성 (필요 시 `/oh-my-claudecode:hud setup`).
- **defaultExecutionMode**: 미설정 (~/.claude/.omc-config.json 없음).
  설정 시 'fast'/'parallel' 키워드 기본 모드 결정.

---

## 9. 푸시 / 릴리즈 상태

- `git push origin main` — **미실행** (109 commits ahead).
- 다음 batch 시작 전 push 권장 — 백업 + CI 통과 확인.
