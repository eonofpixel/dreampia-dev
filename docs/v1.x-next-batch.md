# Dreampia-Dev v1.x — 다음 Batch 로드맵

> **기준일**: 2026-05-07
> **베이스**: v1.4.8 (`560f26e`)
> **목적**: 향후 3-4 batch 동안 진행할 슬롯 정의 + 우선순위 + WHY +
> 의존성 + 예상 시간.

본 문서는 작업 시작 전 슬롯 단위 의사결정의 단일 진실. 새로운 슬롯
추가 시 본 문서를 우선 갱신 후 commit.

---

## 0. 우선순위 원칙

1. **사용자 가시성 (UX)** — 화면에 보이는 것 우선.
2. **데이터 무결성** — schema/migration 은 위험할수록 빨리 처리.
3. **부채 청산** — 절반만 끝난 인프라 (handler, _extra) 마무리.
4. **검증 (test surface)** — 새 코드는 test 출하 강제.

배치 구성 시 **독립 슬롯 5-7 개씩 묶어 ultrapilot 병렬 실행** 가능
하도록 file ownership 충돌 분석 포함.

---

## 1. Batch #1 — 자동화 + Workspace 마무리 (5 슬롯, 독립 그룹 A) ✅ DONE

| 슬롯 | 트랙 | 영역 | 예상 | 결과 |
|---|---|---|---|---|
| **v1.7.26** | 1.7 | Audit log DB persistence | 60min | ✅ `94b6a3f` |
| **v1.6.20** | 1.6 | ChatPanel 잔여 i18n | 30min | ✅ `4e86e25` |
| **v1.7.27** | 1.7 | Automation rule enable/disable toggle | 45min | ✅ `4803f86` |
| **v1.4.9** | 1.4 | Workspace cache invalidation (post-backfill) | 45min | ✅ `6a93b6c` |
| **v1.7.28** | 1.7 | Automation rule import/export JSON | 60min | ✅ `5a843d8` |

**Batch 추정:** 시퀜셜 240min → ultrapilot 병렬 ~80min (3x 가속).
**실제 (2026-05-07):** ulw 모드 (Track A=ChatPanel i18n, Track B=Workspace
cache, Track C=Audit→Toggle→Import/Export 직렬) ~50분. baseline 1988
passed / 7 failed → 2007 passed / 7 failed (+19 신규). 회귀 0.

### v1.7.26 — Audit log DB persistence
**WHY:** v1.7.23~25 에서 handler 가 결과를 audit event 로 emit 하지만
현재는 console.error 만. 영구 저장 + 조회 가능해야 디버깅/모니터링
실용성. `AuditLogStore` 는 v1.0.x 부터 존재 — wire-up 만 필요.

**범위:**
- `getAutomationManager()` 부팅 시 `auditSink` 를 `auditLogStore.recordEvent`
  로 연결.
- `AutomationAuditEvent` 의 `output` / `handler_name` / `error` 를
  `target_json` 으로 직렬화.
- 신규 IPC `automation/audit-log` — 최근 N개 audit event 조회.
- `AutomationModal` 하단에 "최근 실행" 섹션 추가 (선택, v1.7.27 분리 가능).

**파일:** `src/main/index.ts`, `src/main/automation/AutomationManager.ts`,
`src/main/ipc.ts`, `src/main/preload.ts`, `tests/main/automation.audit.test.ts`.

**의존성:** v1.7.25 ✅.

---

### v1.6.20 — ChatPanel 잔여 i18n
**WHY:** v1.7.x 시리즈에서 settings/automation/sidebar 의 i18n 은 모두
완료됐지만 ChatPanel (가장 메인 화면) 의 일부 hardcoded 한국어 문자열
이 남아있음. ko/en 빠진 부분이 사용자 보고 깨끗하지 않게 보임.

**범위:**
- ChatPanel.tsx grep `[가-힣]` 찾아 모든 문자열을 `t()` 통과로 변경.
- 빈 메시지 hint, 시스템 메시지, 에러 메시지 등.
- ko/en pair 추가.

**파일:** `src/renderer/components/chat/ChatPanel.tsx`,
`src/renderer/i18n/messages.{ko,en}.json`,
`tests/renderer/i18n/i18n.coverage.test.ts`.

**의존성:** 없음.

---

### v1.7.27 — Automation rule enable/disable toggle
**WHY:** 현재 rule 은 등록되면 즉시 active. 사용자가 일시 정지하려면
unregister + re-register 필요 → config 잃음. enable 플래그 필수.

**범위:**
- `AutomationRule` + persisted shape 에 `enabled?: boolean` (default true).
- AutomationManager: disabled 면 schedule 등록 X, fire 시 skip + audit.
- IPC `automation/set-enabled(name, enabled)`.
- AutomationModal UI: rule 항목에 toggle switch.
- i18n ko/en 2 키.

**파일:** AutomationManager / settings / ipc / preload / AutomationModal /
i18n / 신규 unit test.

**의존성:** v1.7.25 ✅.

---

### v1.4.9 — Workspace cache invalidation (post-backfill)
**WHY:** v1.4.8 backfill 실행 후 in-memory cache (App.tsx 의 sessions
state, useWorkspace hook) 가 stale. 사용자가 modal 의 [지금 업그레이드]
클릭 후 페이지 reload 없이 새 sha256 id 가 바로 반영되지 않으면 다음
세션 생성 시 conflict 가능.

**범위:**
- `BackfillPromptModal` 의 결과 화면 → `onDone` 시 모든 sessions
  refetch (App.tsx 가 `loadSessions()` 재호출).
- Optional: `app/cache-invalidate` IPC (renderer broadcast 패턴).
- Test: backfill 후 새 createSession 이 sha256 id 로 정상 insert.

**파일:** `src/renderer/components/workspace/BackfillPromptModal.tsx`,
`src/renderer/App.tsx`, 신규 unit.

**의존성:** v1.4.8 ✅.

---

### v1.7.28 — Automation rule import/export JSON
**WHY:** 사용자가 자동화 rule 들을 다른 환경 (다른 PC, 팀 공유) 으로
옮길 수단 없음. settings.json 직접 수정은 위험. UI 에서 [내보내기]
[가져오기] 버튼이 자연스러움.

**범위:**
- IPC `automation/export` — 현재 rules 배열을 JSON 반환.
- IPC `automation/import(json)` — 기존 rules 클리어 + import. handler
  registry validation. 충돌 시 dryrun 결과 반환 후 [확인] 시 적용.
- AutomationModal UI: 헤더에 [내보내기] [가져오기] 버튼.
- 보안: import 는 user confirm 필수.

**파일:** ipc / preload / AutomationModal / i18n / unit test.

**의존성:** v1.7.27 (enabled 플래그도 export 에 포함) — 단 v1.7.27 없이도
가능.

---

## 2. Batch #2 — 사용자 도움 + 인프라 정비 (5 슬롯, 독립 그룹 B) ✅ DONE

| 슬롯 | 트랙 | 영역 | 예상 | 결과 |
|---|---|---|---|---|
| **v1.6.21** | 1.6 | Sidebar 단축키 도움말 panel | 45min | ✅ `303dee7` |
| **v1.4.10** | 1.4 | Workspace UNIQUE(root) 충돌 graceful 처리 | 60min | ✅ `d7764e6` |
| **v1.7.29** | 1.7 | Automation audit viewer panel | 60min | ✅ `2637f22` |
| **v1.6.22** | 1.6 | Chat empty state CTA improvements | 45min | ✅ `6adbad2` |
| **v1.4.11** | 1.4 | Backfill conflict resolution (delete) | 60min | ✅ `9b97174` |

**Batch 추정:** 시퀜셜 270min → ulw 직렬 ~75분.
**실제 (2026-05-07):** ulw 모드 — 메인 세션이 5 슬롯 직렬 처리
(executor agent 가 중간 cut-off 빈번해 직접 처리가 안정적). baseline
2007 passed / 7 failed → 2032 passed / 7 failed (+25 신규). 회귀 0.

### v1.6.21 — Sidebar 단축키 도움말 panel
**WHY:** Mod+,, Mod+K, Esc 등 단축키 존재하지만 사용자 가시 X. SlashHelp
modal 처럼 [?] 또는 Mod+? 로 popup. 발견성 향상.

### v1.4.10 — Workspace UNIQUE(root) 충돌 graceful 처리
**WHY:** v1.4.8 backfill 안 한 사용자가 새 세션 생성 → sha256 id 가 다른
row 와 root 충돌 시 INSERT 실패. 현재는 fatal error. 사용자에게
"이미 등록된 workspace 입니다" + 기존 row 사용 옵션 제공.

### v1.7.29 — Automation audit viewer panel
**WHY:** v1.7.26 의 audit DB persistence 이후 자연스러운 다음. 사용자가
"내 자동화가 어제 잘 동작했나?" 확인 가능. Top-N 최근 / filter by rule_name
/ filter by event type.

### v1.6.22 — Chat empty state CTA
**WHY:** 새 세션 시작 시 "메시지를 입력하세요" 외 CTA 없음. 추천 prompts,
plugins 안내, MCP 서버 빠른 추가 등 진입 도움.

### v1.4.11 — Backfill conflict resolution
**WHY:** v1.4.8 의 conflict (sha256 target 이 이미 다른 row 차지) 는
현재 silently skip. 사용자가 "어떤 workspace 가 충돌했나" 알 수 없음.
Modal 에서 충돌 row 선택 → keep / merge / delete 선택지 제공.

---

## 3. Batch #3 — _extra namespace 정리 (3 슬롯, 순차 그룹 C) — 진행 중

> **위험도 높음** — pipeline:refactor 모드 권장. 한 번에 batch 보다
> 슬롯 1개씩 architect 검증 후 진행.

| 슬롯 | 트랙 | 영역 | 예상 | 결과 |
|---|---|---|---|---|
| **v1.8.0** | 1.8 | _extra namespace usage audit | 90min | ✅ `fed5b2a` |
| **v1.8.1** | 1.8 | High-frequency _extra fields → column promote | 120min | ✅ `5f72aa7` (permission.default_level + plan.active) |
| **v1.8.2** | 1.8 | _extra schema 문서화 + lint rule | 60min | pending |

### v1.8.0 — _extra namespace usage audit
**WHY:** v1.4.4-v1.4.6 에서 도입된 `_extra.{workspace|terminal|browser|plan|permission}`
는 schema-less JSON 으로 시작. 일부 필드는 자주 query 됨 (column 으로
promote 필요), 일부는 array 라 JSON 유지가 적합. 어떤 필드가 어떻게
쓰이는지 audit 후 promote 결정.

**범위:**
- `_extra` 모든 caller grep + frequency count.
- 각 namespace 별 schema 추론 (TypeScript type 또는 zod).
- 결과 → `docs/extra-namespace-audit.md` (read-only deliverable).
- 코드 변경 X — 다음 슬롯의 input.

### v1.8.1 — _extra columns promote
**WHY:** v1.8.0 의 audit 결과 기준 자주 query 되는 scalar 필드를
column 으로 promote. JSON 추출 cost 제거 + 인덱스 가능.

**범위:**
- Migration N+1 — 각 promote 대상 column ADD.
- Backfill: 기존 row 의 `_extra.X` → column 복사.
- Read 측: `_extra.X` 우선 column → fallback `_extra.X`.
- Write 측: column 직접.
- 다음 batch 에서 _extra.X 삭제 (v1.8.2 또는 v1.9.x).

### v1.8.2 — _extra schema 문서화 + lint rule
**WHY:** v1.8.1 후에도 `_extra` 는 array/object 필드용 합법. 어떤
필드가 어디 사는지 명확한 문서 + 새 PR 에서 `_extra.X` 추가 시
lint 가 schema 등록 강제.

---

## 4. 의존성 그래프

```
Batch #1 (독립 5)
  v1.4.9 → v1.4.10 → v1.4.11   (v1.4 트랙 sequential)
  v1.6.20 → v1.6.21 → v1.6.22  (v1.6 트랙 sequential)
  v1.7.26 → v1.7.27 → v1.7.28 → v1.7.29  (v1.7 트랙 sequential)

Batch 간 dependency:
  v1.4.9 ← v1.4.8 (sha256 default) ✅
  v1.7.26 ← v1.7.25 (handler picker) ✅
  v1.7.29 ← v1.7.26 (audit DB)
  v1.4.10 ← v1.4.9
  v1.4.11 ← v1.4.10

Batch #3 (순차):
  v1.8.0 (audit) → v1.8.1 (promote) → v1.8.2 (lint)
```

---

## 5. 모드 매핑 (어떤 OMC 모드로 진행)

| Batch | 권장 모드 | 이유 |
|---|---|---|
| **#1 (독립 5)** | `ultrapilot` | 5 worker file ownership, 3x 가속 가능. 슬롯 간 충돌 거의 없음. |
| **#2 (독립 5)** | `ultrapilot` 또는 `ecomode` | #1 과 유사. ecomode 는 토큰 절감 우선. |
| **#3 (순차 위험)** | `pipeline:refactor` 또는 `ralph` | architect 검증 + critic 가 필수. v1.8.1 은 migration 이라 신중. |

**Default 추천:** `ecomode` (저비용 50/50 분포 매칭) + 위험 슬롯에만
`pipeline` 또는 `ralph` 선택적 활성.

---

## 6. 시작 결정 매트릭스

다음 세션 시작 시 사용자 의사결정 트리:

```
질문 1: 시간 여유?
  >2시간 ───┬── 그렇다면 → Batch #1 ultrapilot
            └── 아니다 → 슬롯 1개 ecomode

질문 2: 위험 감수?
  높음 ───┬── 그렇다면 → Batch #3 (v1.8.x) 시작
          └── 낮음 → Batch #1 또는 #2

질문 3: 사용자 가시성?
  중요 ───┬── 그렇다면 → v1.6.20 / v1.6.22 / v1.7.27 (UI)
          └── 인프라 우선 → v1.7.26 / v1.4.9
```

**기본 권장 (안전 + 진척도):**
> Batch #1 ultrapilot 실행 → v1.7.26 + v1.6.20 + v1.7.27 + v1.4.9 + v1.7.28
> 동시 진행, 약 80-100분 예상.

---

## 7. 슬롯 추가 / 변경 시

새 슬롯이 발견되거나 기존 슬롯 우선순위가 바뀌면:

1. 본 문서의 해당 batch 섹션에 추가/수정.
2. WHY / 범위 / 파일 / 의존성 명시.
3. 의존성 그래프 갱신.
4. commit (`docs(roadmap): v1.x next batch — slot X 추가`).

---

## 8. 메타 — 진행 추적

- 각 슬롯 완료 시 본 문서의 "예상" 옆에 ✅ + 실제 시간 추가.
- `docs/status-{date}.md` 를 batch 끝에 새로 작성 (스냅샷 패턴).

---

## 9. 참고 문서

- `docs/status-2026-05-07.md` — 현재 작업 현황 스냅샷.
- `docs/v1.x-roadmap.md` — 전체 v1.x 비전 (P0/P1/P2/P3 segmentation).
- `CHANGELOG.md` — 모든 commit 의 상세 변경 내역.
