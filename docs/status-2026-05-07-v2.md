# Dreampia-Dev — 작업 현황 스냅샷 (v2 — Batch #1~#3 완료)

> **기준일**: 2026-05-07 (저녁)
> **마지막 commit**: `4c7e131` (v1.8.3 — _extra dual-write read 제거)
> **package.json**: `1.8.3`
> **origin/main 대비**: +6 commits (v1.8.0~v1.8.3 + 2 docs) — push 직후 상태
> **CHANGELOG.md**: 약 5,500 lines

본 문서는 v1 버전 (`docs/status-2026-05-07.md`) 의 후속 — Batch #1~#3
가 모두 land 한 시점의 스냅샷.

---

## 1. 본 세션 (2026-05-07) 에서 land 한 슬롯 (15)

### Batch #1 — 자동화 + Workspace 마무리 (5 슬롯)
| 슬롯 | 영역 | commit |
|---|---|---|
| v1.4.9 | Workspace cache invalidation post-backfill | `6a93b6c` |
| v1.6.20 | ChatPanel 잔여 i18n | `4e86e25` |
| v1.7.26 | Audit log DB persistence + IPC 조회 | `94b6a3f` |
| v1.7.27 | Automation rule enable/disable toggle | `4803f86` |
| v1.7.28 | Automation rules JSON import/export | `5a843d8` |

### Batch #2 — 사용자 도움 + 인프라 정비 (5 슬롯)
| 슬롯 | 영역 | commit |
|---|---|---|
| v1.6.21 | Sidebar 단축키 도움말 진입점 가시화 | `303dee7` |
| v1.6.22 | Chat empty state CTA chips | `6adbad2` |
| v1.7.29 | Automation audit viewer panel | `2637f22` |
| v1.4.10 | Workspace UNIQUE(root) 충돌 graceful fallback | `d7764e6` |
| v1.4.11 | Backfill conflict resolution UI | `9b97174` |

### Batch #3 — `_extra` namespace 정리 (4 슬롯, audit + promote + cleanup)
| 슬롯 | 영역 | commit |
|---|---|---|
| v1.8.0 | _extra namespace usage audit (read-only doc) | `fed5b2a` |
| v1.8.1 | column promote (permission.default_level + plan.active) | `5f72aa7` |
| v1.8.2 | _extra strict schema + contract test | `17eaf08` |
| v1.8.3 | dual-write read 제거 (column 만 source of truth) | `4c7e131` |

**누적 변화**: 14 feature commits + 4 docs commits = 총 18 commits.
신규 unit: +57 tests (Batch #1: 19 + Batch #2: 25 + Batch #3: 13).
typecheck clean, lint clean.

---

## 2. 트랙 구조 업데이트 (v1.x)

| 트랙 | 영역 | 마지막 슬롯 | 상태 | 비고 |
|---|---|---|---|---|
| **v1.0.x** | Core stability | v1.0.15 | 🟢 closed | |
| **v1.1.x** | Permission + plugin | v1.1.28 | 🟢 closed | |
| **v1.2.x** | Direct API foundation | v1.2.5 | 🟢 closed | |
| **v1.3.x** | A11y + i18n + automation infra | v1.3.7 | 🟢 closed | |
| **v1.4.x** | Storage debt (B-1~B-4) + workspace UX | **v1.4.11** | 🟡 active | 다음: 비정규화 정리 (grants, plan_items) |
| **v1.5.x** | Media + Vision + Direct API UI | v1.5.7 | 🟢 closed | |
| **v1.6.x** | Browser + Plugin hooks + Chat UX | **v1.6.22** | 🟡 active | 다음: 추가 i18n 또는 plugin loader UI |
| **v1.7.x** | Telemetry + Automation + Settings | **v1.7.29** | 🟡 active | 다음: webhook listener, audit retention |
| **v1.8.x** | _extra namespace 정리 (B-3 후속) | **v1.8.3** | 🟡 active | 다음: v1.8.4 _extra write 제거 |

---

## 3. 테스트 baseline

| 지표 | 직전 (v1) | 현재 (v2) | 차이 |
|---|---|---|---|
| **passed** | 1,988 | 2,045 | +57 |
| **failed (baseline)** | 7 | 7 | 0 |
| **test 파일 수** | 175 | ~183 | +8 |

**baseline 7 fail 의 정체**:
- workspace pick (Windows path 처리) 5개 — 이번 작업과 무관.
- SessionStore migration 2개 — `expect(getSchemaVersion()).toBe(5)` 였으나
  실제는 v=15. 단순 fixup 가능 (하지만 본 batch 범위 외).

---

## 4. 작업 트리 상태

```
M tests/storage/permissionGrants.idText.test.ts (pre-existing dirty, 무관)
?? settings.json (untracked build artifact, .gitignore 필요)
```

본 세션의 모든 변경은 commit 됨. 위 두 항목은 다음 세션에서 cleanup
가능 (예: `.gitignore` 에 settings.json 추가, permissionGrants test
revert 또는 정식 land).

---

## 5. v1.8.3 후속 — column-only transition 마무리 (제안 v1.8.4)

v1.8.3 까지 완료된 상태:
- ✅ Migration 015: column 추가 + backfill.
- ✅ INSERT 시 dual-write (column + `_extra`).
- ✅ assembleSession: column 만 read.
- ⏸ buildStoredMetadata: `_extra` 측 write 유지 (한 슬롯 더 backward compat).

**v1.8.4 제안**:
1. `buildStoredMetadata` 가 `_extra.permission.default_level` /
   `_extra.plan.active` 를 더이상 직렬화하지 않음.
2. `MetadataExtraSchema` 의 해당 필드 optional 또는 제거.
3. 향후 down-grade 시나리오 위해 한 버전 더 dry-run 가능.
4. 신규 unit: insert 후 metadata_json 에서 두 필드 부재 검증.

---

## 6. 다음 batch 후보 (v1.8.4 외)

### A. 트랙별 다음 슬롯
- **v1.4.12+**: `_extra.permission.grants[]` ↔ `permission_grants` 테이블 비정규화 정리. metadata 캐시 제거 + DB FK 만 사용.
- **v1.4.13+**: `_extra.plan.checklist[]` ↔ `plan_items` 동일 정리.
- **v1.6.23+**: ChatPanel performance — turn rendering virtualization (대형 세션).
- **v1.7.30+**: Automation webhook listener 본 구현 (현재 stub).
- **v1.7.31+**: Audit log retention + cleanup (현재 무한 grow).

### B. 시스템적 정리
- **baseline 7 fail 정리** — workspace pick path 정리 + SessionStore migration test fixup. 이걸로 baseline `0/0` 시작 가능.
- **status snapshot 자동화** — git hook 또는 npm script 로 `docs/status-{date}.md` 자동 갱신.

### C. 다음 batch 묶음 제안 (Batch #4)
v1.8.4 + 비정규화 (v1.4.12 / v1.4.13) + baseline 7 fail 정리.
ulw 모드 권장. 예상 ~150min.

---

## 7. 다음 세션 진입 prompt (참고)

```
Dreampia-Dev 작업 재개. 직전 status: docs/status-2026-05-07-v2.md.

마지막 commit: 4c7e131 (v1.8.3). 베이스라인 2045/7.

다음 batch 후보:
- v1.8.4 — _extra write 제거 (column-only transition 마무리)
- v1.4.12 — permission.grants 비정규화 정리
- v1.4.13 — plan.checklist 비정규화 정리
- baseline 7 fail 정리 (workspace pick path + SessionStore migration v=5)

ulw 모드로 위 4개 슬롯 진행 (기존 ulw 패턴 — 메인 세션 직렬,
실측 ~150min). docs/v1.x-next-batch.md 의 Batch #4 슬롯 추가 후
로드맵 갱신 + commit + push 까지 포함.

자동 진행 시작.
```

---

## 8. 참고 파일

- `CHANGELOG.md` — 모든 commit 의 상세 변경.
- `docs/v1.x-next-batch.md` — Batch #1~#3 ✅ 마킹, Batch #4 미정의.
- `docs/extra-namespace-audit.md` — v1.8.0 의 promote 의사결정 input.
- `docs/extra-namespace-schema.md` — v1.8.2 의 strict schema canonical.
- `docs/bootstrap-prompt.md` — 다음 세션 prompt 템플릿 3종.
