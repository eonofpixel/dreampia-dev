# Dreampia-Dev v1.x Roadmap — FINAL

> **상태**: v1.0.6 (commit 8d65512) 기준 종합 audit + 로드맵.
> **작성**: 2026-05-04, Playwright drive harness (5 스펙 × 33 시나리오) +
> spawnSafe unit (11 시나리오) + Codex 0.125.0 외부 검토 반영.
> **검증 방식**: 직접 control loop 으로 source 검토 + 스크린샷 검증 +
> Codex 가 sample 검토 후 9개 새 gap 발견.

---

## 0. Executive Summary (3 줄)

1. **코어 라인 (CLI 통합 + 채팅 + 영속화) 가동.** 한국어 폴더 / Korean
   workspace cwd 처리까지 v1.0.4-1.0.6 에서 production-grade 회귀 lock.
2. **그러나 25+ 군데 가짜 완성** — schema 만 있고 UI/IPC 없는 기능들
   (audit_log 자동 기록, panel_visible 토글, Compare 모드, ··· 더보기,
   permission grant UI 자체).
3. **v1.0.6 의 drift badge 가 ChatHeader layout 회귀 도입** —
   `r5-23b-drift-detected.png` 가 증거. 다음 버전에서 즉시 책임 청산.

**Codex 가 추가 검출한 critical 항목**:
- 보안: symlink path guard 부재 (`workspace/read-file`)
- 가짜 완성: permission grant UI 자체 부재 (Settings 는 있는 척)
- 의존성: hard limit 이 placeholder pricing 위에서 의미 없음

---

## 1. 현재 상태 (v1.0.6 / commit 8d65512)

### 1.1 작동하는 것 (verified)

| 영역 | 상태 | 검증 |
|------|------|------|
| CLI 통합 (Claude / Codex) | ✅ | 사용자 직접 + drive3 streaming |
| 한국어 폴더 cwd 처리 | ✅ | spawnSafe unit 11/11 + drive2 fold change |
| Mock provider fallback | ✅ | drive3 streaming round-trip |
| FTS5 검색 | ✅ | drive2 search input |
| 영속성 (SQLite migrations 001~005) | ✅ | drive3 reload restore |
| 사이드바 / ChatHeader / PreviewPanel 3-panel | ✅ | drive1 정적 visual |
| 온보딩 wizard | ✅ | drive3 onboarding-reopen |
| @ 멘션 popover | ✅ | drive3 with file matches |
| 단축키 (Mod+,, Mod+K, Esc) | ✅ | drive3 |
| 사용량 패널 (오늘/7일/30일, CSV) | ✅ | drive4 |
| 진단 패널 | ✅ | drive4 |

### 1.2 가짜 완성 / Stub / TODO 인벤토리 (DRAFT + Codex 발견 통합)

#### 1.2.1 보안 영역 (가장 critical)

| ID | 항목 | 위치 | 영향 |
|----|------|------|------|
| **SEC-1** | **`workspace/read-file` path guard 가 `path.resolve` prefix 만 검사 — `realpath`/symlink 미검사** | `src/main/ipc.ts:403`, `935-952` | symlink/junction 으로 **workspace 외부 파일 읽기 가능**. 기존 traversal test 는 `../..` 만 커버. |
| **SEC-2** | **Permission grant UI/modal/IPC 자체 부재** | `src/tools/Queue.ts:360` | i18n 은 "사용자 승인" 있는 척, 실제 queue 는 `requires_user_confirmation` → 즉시 deny. **사용자가 권한 관리 가능한 줄 알지만 실제는 0**. |
| **SEC-3** | audit_log 자동 기록 미연결 (테이블만 존재) | `001_init.sql:124` | 보안/회계 trail 없음 |
| **SEC-4** | tool result `side_effects: never[]` placeholder | `src/tools/types.ts:192` | 실제 부작용 구조화 X → audit/replay/permission review 데이터 무결성 약함. **SEC-3 의존성** |

#### 1.2.2 가짜 완성 (UI 있지만 작동 X 또는 backend 만)

| ID | 항목 | 위치 |
|----|------|------|
| FAKE-1 | panel_visible 토글 (schema/state 만, UI/IPC 0) | `browser.ts:67`, `App.tsx:114` |
| FAKE-2 | Compare 모드 UI 진입점 (orchestrator 백엔드만) | (사이드바에 없음) |
| FAKE-3 | ChatHeader ··· (more) — `<span>` 장식, 핸들러 0 | `ChatPanel.tsx:391` |
| FAKE-4 | leader election hook 존재, production 미연결 (단일 인스턴스 lock 강제) | `useLeaderElection.ts:7`, `main/index.ts:329-336`, `multi-instance.spec.ts:31 skip` |
| FAKE-5 | MCP input_schema validation (`z.unknown()` 통과) | `McpManager.ts:236` |

#### 1.2.3 비용 / 한도 영역

| ID | 항목 | 위치 |
|----|------|------|
| COST-1 | placeholder pricing — unknown model = `0` 비용 기록 | `src/providers/pricing.ts:8, 103` |
| COST-2 | hard limit 이 COST-1 위에서 의미 없음 (의존성) | (의존 관계) |

#### 1.2.4 워크스페이스 일관성 (v1.0.5 fix 누락 영역)

| ID | 항목 | 위치 |
|----|------|------|
| WS-1 | `/compare` slash command 가 v1.0.5 workspace drift fix 누락 — `activeSession.workspace.root` 사용 | `src/renderer/App.tsx:809` |

#### 1.2.5 멘션 / 입력 영역

| ID | 항목 | 위치 |
|----|------|------|
| MENT-1 | `@file` mention 개수 제한 / dedupe / cumulative byte limit 없음 (DoS 위험) | `src/renderer/mentions/resolver.ts:80` |

#### 1.2.6 테스트 / 검증 인프라

| ID | 항목 | 위치 |
|----|------|------|
| TEST-1 | drive harness 가 깨진 ChatHeader 통과시킴 (height/nowrap/bbox/diff 검증 X) | `e2e/_drive5.spec.ts:48` |
| TEST-2 | 19 unit fail (orchestrator, ipc.compare, ipc.lock 등) — VS Build Tools 부재로 로컬에서만 실패 (CI 통과) | (env) |

#### 1.2.7 Provider / API

| ID | 항목 | 위치 |
|----|------|------|
| API-1 | Claude/Codex Adapter SSE 파싱 stub (`return []`) | `*Adapter.ts` |
| API-2 | Codex tool_use translation best-effort | `translateCodexJsonl.ts:64` |

#### 1.2.8 메타 / 운영

| ID | 항목 | 위치 |
|----|------|------|
| META-1 | **package.json version drift — `1.0.5`인데 HEAD commit 은 `v1.0.6` 자칭** | `package.json:3`, `CHANGELOG.md:5` |
| META-2 | Sentry crash report 미통합 | `main/index.ts:123` |
| META-3 | Down-migration 부재 | `migrate.ts:7` |
| META-4 | userData = workspace 방어 경고 부재 | (없음) |
| META-5 | VS Build Tools 가이드 README/CONTRIBUTING 부재 | (없음) |

### 1.3 v1.0.6 도입 회귀 (UX bug)

| 회귀 | 위치 | 증거 |
|------|------|------|
| **UX-REG-1**: ChatHeader layout 폭삭 (drift badge 가 모든 요소 wrap 유발) | `ChatPanel.tsx:380-391` (한 줄 header, `shrink-0`/`whitespace-nowrap`/`min-w-0` 부재) | `r5-23b-drift-detected.png` |
| UX-REG-2: 세션 제목 "새 채팅 1" → "새 채..." truncate | 동일 | 동일 |

### 1.4 PreviewPanel 의 의도된 deferred (P2/P3)

`PreviewPanel.tsx:23-27` 에 명시:
- Annotation mode (DOM Inspector)
- Screenshot / DOM dump
- Side chat tabs (parent-child fork)
- Fullscreen overlay layout

### 1.5 Architectural Debt (001_init.sql 헤더 명시)

```
1. workspaces.root UNIQUE vs Session.workspace_id 비결정적 → INSERT 충돌 위험
2. permission_grants.id INTEGER AUTOINCREMENT vs UUIDv7 string (JSON wrapper)
3. JSON-wrapped 잔여 필드 (history_json, target_json, metadata_json._extra)
4. Down-migration 부재
```

### 1.6 사이드바 "준비 중"

- Sidebar [플러그인] — Plugin Loader 미구현
- Sidebar [자동화] — Bot Automation 미구현

### 1.7 멀티모달 (revert 된 v1.0.1 stub)

- mediaConstants.ts (삭제됨)
- ImageReferenceBlockSchema (제거됨)
- PdfReferenceBlockSchema (제거됨)

---

## 2. Roadmap (Codex 검토 반영 최종)

### 🔥 P0 — 즉시 정리 (v1.0.7 ~ v1.0.13, ~3주)

가짜 완성 + UX 회귀 + 보안. 사용자 신뢰도 직격탄.

#### v1.0.7 — UX 회귀 책임 청산 (제가 만든 것)
**작업량: 1-2일**
- UX-REG-1: ChatHeader 재설계 — `flex-shrink-0`, `whitespace-nowrap`, `min-w-0`, drift badge 짧은 형태 (⚠ icon + tooltip)
- UX-REG-2: 세션 제목 max-width 정책
- FAKE-3: ··· (more) 진짜 menu (사이드바 [플러그인] 패턴 OR 진짜 dropdown)
- META-1: **package.json `1.0.5` → `1.0.6` 동기화** (1-2시간)
- TEST-1: drive harness 강화 — header height assert + screenshot diff (`@playwright/test toHaveScreenshot`) (1-2일)

#### v1.0.8 — Panel 토글 시스템
**작업량: 1-2일**
- FAKE-1: panel_visible 의 IPC + UI 추가 (`browser/set-panel-visible` 채널, ChatHeader 의 [👁] 버튼, `Mod+B` 단축키)
- Sidebar 토글 (`Mod+\`)
- Narrow window auto-collapse (< 1024px)

#### v1.0.9 — 보안 영역 SEC-1, SEC-2 (보안 hole 청산)
**작업량: 3-5일** ⚠ 가장 큰 P0
- SEC-1: `workspace/read-file` path guard — `realpath` resolution + symlink reject (Windows junction 포함)
- SEC-2: Permission grant UI/modal/IPC 본격 구현
  - 승인 modal (capability + target + grant duration)
  - grant 저장 IPC (`permission/grant` 채널 + permission_grants insert)
  - grant 관리 UI (Settings > 권한 탭에 list view)
  - i18n 정합 (지금 거짓말하는 라벨 정정)

#### v1.0.10 — audit_log + side_effects 묶음 (의존성)
**작업량: 2-3일**
- SEC-3 + SEC-4: 함께 해야 함
  - tool result `side_effects` 구조 정의 (file/process/network)
  - audit_log 자동 기록 (permission grant + tool_use complete/fail)
  - Settings > 진단 또는 [감사] 탭에 audit_log viewer

#### v1.0.11 — 비용 / 한도 (의존성)
**작업량: 2-3일**
- COST-1: pricing source 통합 (Anthropic / OpenAI 공식 가격 fetch + cache + stale 표시)
- unknown model policy: `0` 대신 명시적 "unknown" 마커 + 사용자 알림
- COST-2: hard limit (이젠 진짜 동작) — 한도 도달 시 차단 modal

#### v1.0.12 — 가짜 완성 마무리
**작업량: 2-3일**
- FAKE-2: Compare 모드 진위 결정 — 활성화 OR 명시적 "준비 중" + 19 unit fail 일부 정리
- FAKE-4: leader election 결정 — production 연결 OR 코드 정리 + multi-instance.spec skip 해제 OR 명시적 single-instance 표시
- FAKE-5: MCP input_schema validation (`z.unknown()` → 실제 schema)
- WS-1: `/compare` workspace drift fix (`defaultWorkspace?.root ?? activeSession.workspace.root` 통일)

#### v1.0.13 — 입력 / 멘션 영역 + 메타
**작업량: 1-2일**
- MENT-1: @file mention rate-limiting (총 개수 50, dedupe, cumulative 200KB)
- META-4: userData = workspace 방어 경고 (사용자가 본인 userData 폴더 고르면 dialog)
- META-5: README/CONTRIBUTING 의 VS Build Tools 가이드 + Korean 폴더 가이드

**P0 합계**: 약 3주 (15-20 작업일)
**P0 종료 조건**: SEC-* 0. UX-REG-* 0. CHANGELOG 의 자랑이 코드와 일치.
사용자가 "이 도구 안전하다" 인식 가능.

### 🎯 P1 — 사용자 직접 신경쓴 영역 + 확장성 (v1.1.x, ~3주)

#### v1.1.0 — SEC-2 full (DONE)
**SEC-2 full Permission grant UI/modal/IPC** — Codex Q6 picking 그대로.
- Queue async pause-resume + IpcPermissionConfirmer + 60s timeout + grant 영속.
- PermissionApprovalCard inline + PermissionDangerModal center modal.
- Settings > 권한 grants table + revoke.

#### v1.1.1 — SEC-2 hotfix (DONE)
**Codex Q7 strict blind spot 청산**.
- 'session' grant DB 영속 → in-memory only.
- High-risk capability 강제 escalation + server-side downgrade.

#### v1.1.2 — SEC-2 hotfix (DONE)
**Codex Q8 strict blind spot 청산**.
- `sessionGrants` 키 SessionId → webContentsId (renderer spoof 방어).
- BrowserWindow 'closed' + app 'before-quit' cleanup hook.
- DangerModal `once_only_notice` 배너.

#### v1.1.3 — Real CLI integration e2e (Codex Q5/Q8 권고)
**작업량: 5일**
- 3-tier mock: vi.mock(child_process) PR / fake binary integration / nightly real CLI.
- VCR fixture: manual record only, PR diff 동봉 (`npm run vcr:record`).
- drive14 (Claude CLI tool_use round-trip), drive16 (failure modes), drive17 (VCR drift) 필수.
- drive15 (한국어 cwd + streaming) 2순위.

#### v1.1.4 — Workspace UX 마무리
**작업량: 3-4일**
- Auto-new-chat prompt: 폴더 변경 시 다이얼로그.
- Per-session sticky workspace lock: ChatHeader 🔒 toggle, `sessions.workspace_locked` migration 007.
- drift badge → 진짜 menu.

#### v1.1.5 — Plugin Loader MVP
**작업량: 1주**
- `~/.dreampia/plugins/<name>/index.js` 로딩.
- Hook 시스템 (pre-turn, post-turn, route-override).
- Sandbox + capability grant (SEC-2 인프라 활용).
- 첫 example: cost-limit-hook.

#### v1.1.6 — Loading / Error / Empty + visual polish
**작업량: 1-1.5주**
- Loading skeleton (세션 로드, 폴더 변경, MCP 연결).
- Error toast 시스템 + 재시도 버튼.
- Empty state + 색상 paradigm 통일.
- Mock / Claude CLI / Codex CLI badge 일관화.

**P1 합계**: 약 3주 잔여 (v1.1.3~v1.1.6).

### 🚀 P2 — 멀티모달 + Direct API + PreviewPanel (v1.2.x, ~6-8주)

별도 세션 + Codex roadmap 재평가 후 진입 권장.

#### v1.2.0 ~ v1.2.1 — Image / PDF (2.5주)
- mediaConstants.ts 부활
- 채팅 입력 PNG/JPG drag-drop / paste
- PDF 텍스트 추출 + 미리보기
- 이미지 영속화 설계 (`~/.dreampia/media/`, max size + LRU)
- vision API 라우팅

#### v1.2.2 ~ v1.2.3 — Direct API mode (1.5주)
- Anthropic / OpenAI SSE 파싱 실제 구현 (API-1)
- API key 입력 UI
- provider auto-routing (CLI > API)

#### v1.2.4 ~ v1.2.5 — PreviewPanel 본격화 (2주)
- Annotation mode (DOM Inspector)
- Screenshot / DOM dump → AI context
- Side chat tabs (parent-child fork)
- Fullscreen layout

### 🛠 P3 — 기술 부채 / A11y / 운영 (v1.3.x, ~5-7주)

#### v1.3.0 ~ v1.3.3 — Schema architectural debt (~2주)
- B-1: workspace_id deterministic
- B-2: permission_grants TEXT promote
- B-3: JSON-wrapped → column promote (multi-step)
- B-4: down-migration

#### v1.3.4 — A11y full audit (~1주)
- focus-visible 통일
- aria-live region (스트리밍)
- 장식 element 정리
- `@axe-core/playwright` 통합
- 색 대비 WCAG AA

#### v1.3.5 — 다크모드 회귀 (~3일)
- drive 스펙에 테마 토글 시나리오
- 시스템 테마 자동 감지

#### v1.3.6 — 운영 안정성 (~1주)
- META-2: Sentry 통합
- 사용자 텔레메트리 옵트인
- M-4: Onboarding step 5 추천 prompts 클릭 동작
- Provider selection UI 명시화

#### v1.3.7 — Bot Automation 본격 (~1-2주)
- 시간 기반 trigger
- webhook trigger
- Sidebar [자동화] 활성화

---

## 3. Total Estimate

| Phase | 작업 일수 | 인력 | 기간 |
|-------|---------|------|------|
| P0 | 15-20 일 | 1명 | **3주** |
| P1 | 22-27 일 | 1명 | **4-5주** |
| P2 | 38-50 일 | 1-2명 | **6-9주** |
| P3 | 32-45 일 | 1-2명 | **5-7주** |
| **합계** | **107-142 일** | **1-2명** | **~5-7개월** |

---

## 4. 의존성 그래프 (P0 내부)

```
v1.0.7 (UX 회귀)         ─── 독립
v1.0.8 (Panel 토글)      ─── 독립
v1.0.9 (SEC-1, SEC-2)    ─── SEC-2 가 v1.0.10 의 prerequisite
v1.0.10 (audit + side_effects) ─── SEC-2 의존, COST-2 prerequisite
v1.0.11 (pricing + hard limit) ─── COST-1 → COST-2
v1.0.12 (FAKE 정리)       ─── 독립
v1.0.13 (메타)            ─── 독립
```

**핵심 의존**:
- audit_log 의 `permission_grant` 기록은 SEC-2 (grant 자체) 가 있어야 작동
- COST-2 hard limit 은 COST-1 (실제 pricing) 가 있어야 의미 있음
- side_effects 구조화 (SEC-4) 는 audit_log 의 데이터 무결성 prerequisite

---

## 5. Risk

| ID | Risk | 완화 |
|----|------|------|
| R-1 | SEC-2 (permission grant UI) 가 2-4일 추정인데 실제 3-5일 가능 (UX 디자인 + i18n + e2e 다 해야 함) | 첫 day 에 minimal grant flow 만 wire-up, 그 다음 polish |
| R-2 | B-3 (JSON column promote) 데이터 손실 가능 | B-4 (down-migration) 먼저 + dry-run + 사용자 backup 안내 |
| R-3 | I-* (Image/PDF) 가 사용자 disk 빠르게 채움 | I-5 (영속화) 시 max size + LRU + 사용자 통제 |
| R-4 | J-* (Direct API) 가 비용 폭주 | COST-2 (hard limit) 가 J-* 와 같은 시점에 활성화 |
| R-5 | K-* (Plugin) 의 보안 (untrusted code) | sandbox / capability list / signing |
| R-6 | L-* (PreviewPanel 본격화) 의 Electron 버전 호환성 | Electron 33 → 34/35 마이그레이션 시점에 동시 |
| **R-7 (Codex)** | **TEST-1 (drive harness 검증 부재)** 가 v1.0.7 들어가기 전에 안 되면 v1.0.7 의 UX 수리도 회귀 못 잡음 | v1.0.7 의 첫 작업이 TEST-1 강화여야 함 (header height assert + screenshot diff) |

---

## 6. Open Questions

1. Compare 모드 진위 결정 — 활성화 vs "준비 중"
2. Plugin 보안 모델 — NPM-style ad-hoc / 서명 plugin / sandbox
3. Image 영속화 — filesystem vs SQLite BLOB
4. Direct API mode default — CLI > API 자동 vs 사용자 선택
5. Multi-window leader election 진위 — 활성화 vs 명시적 single-instance
6. Bot Automation scope — cron / webhook / 둘 다

---

## 7. 추천 첫 한 발

```
🥇 v1.0.7 (UX 회귀 + version sync + drive harness 강화)
   ↓ 1-2일
🥈 v1.0.9 (SEC-1 + SEC-2 — symlink path guard + permission grant UI)
   ↓ 3-5일 (P0 중 가장 큰 묶음, 가장 critical)
🥉 v1.0.10 (audit + side_effects)
   ↓ 2-3일
```

**P0 가장 critical 한 것은 보안 영역 (SEC-1, SEC-2).** 사용자가 개발자에게
신뢰를 잃을 수 있는 가장 큰 risk. v1.0.7 (UX 회귀) 청산 후 즉시 SEC 작업
권장.

---

## 8. Codex 검토 요약 (반영됨)

Codex 0.125.0 가 read-only 환경에서 source 검토 후 발견한 9개 새 gap 모두
이 plan 에 통합:

| Codex 발견 | 통합 위치 |
|-----------|----------|
| /compare workspace drift fix 누락 | WS-1 / v1.0.12 |
| workspace/read-file symlink guard 부재 | SEC-1 / v1.0.9 |
| Permission grant UI/IPC 자체 부재 | SEC-2 / v1.0.9 |
| leader election production 미연결 | FAKE-4 / v1.0.12 |
| pricing placeholder | COST-1 / v1.0.11 |
| @file mention rate-limit 부재 | MENT-1 / v1.0.13 |
| tool result side_effects placeholder | SEC-4 / v1.0.10 |
| drive harness 시각 회귀 검증 부재 | TEST-1 / v1.0.7 |
| package.json version drift | META-1 / v1.0.7 |

---

## 9. 다음 행동

```
v1.0.7 시작 시:
  1. e2e/_drive5.spec.ts 에 ChatHeader height assert + nowrap 검증 추가
  2. ChatHeader 재설계 (flex-shrink-0 + whitespace-nowrap + min-w-0)
  3. drift badge → ⚠ icon only + tooltip
  4. ··· (more) → 진짜 dropdown menu 또는 제거
  5. package.json version 1.0.5 → 1.0.6
  6. CHANGELOG.md 에 v1.0.6 entry 추가
  7. drive 회귀 통과 확인
  8. typecheck + lint + 11/11 spawnSafe + 33/33 drive PASS
  9. commit + tag
```

`v1.0.7` 작업 시작 명령:

```bash
git checkout -b v1.0.7-ux-cleanup
# … 작업 …
git commit -m "fix(ui,version): v1.0.7 — ChatHeader regression + version sync + drive harness 강화"
```

---

**이 plan 은 v1.0.6 시점의 audit. v1.0.7 시작 시 또는 P0 완료 후 갱신
필요.** Codex re-review 권장 시점: P0 완료, P2 시작 직전.
